# =============================================================================
# 환경변수 템플릿
# =============================================================================
# 이 파일을 복사하여 .env 파일을 생성하세요
# cp .env.example .env
#
# .env 파일은 .gitignore에 의해 Git에 올라가지 않습니다 
# =============================================================================

# 이 폴더는 빈 .env 폴더 생성을 위한 파일이지 여기다가 환경변수 중요 내용 넣지 말기
# 보안 상 이 빈 파일을 위의 절차로 .env를 생성하고 거기다 넣기